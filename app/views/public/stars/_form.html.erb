<%# 平均点を算出し、round関数で切り上げ %>
<% if @stars %>
星：<%= @stars.average(:star).to_f.round(1) %>

<%# 評価数を星に置き換える %>
 <div class="average-review-rating" data-score=<%= @stars.average(:star) %>></div>
 <% end %>

<th class="table__col1">総合評価</th>
 <td class="table__col1">  <div class="field" id="star"><% f.label :star %></div></td>
</tr>
<%# 同様に、rating1なども記載する%>

<%= render partial: "stars/form_rate", locals: { bento_box: @bento_box } %>