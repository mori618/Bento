<div class="container-xxl p-5">
  <div class="row text-center">
    <div class="col-md-2">
      <% if @genres.present? %>
        <%= render 'public/searches/genre.html.erb' %>
      <% end %>
    </div>
    <div class="col-md-10">
      <div class="title-body">
        <h2>弁当一覧(<%= @genre.name %>)</h2>
      </div>

      <div class="max-box d-flex flex-wrap justify-content-center ">
        <% if @bentoes.present? %>
          <% @bentoes.each do |bento| %>
            <div class="card m-3" style="width: 18rem;">
              <%= image_tag bento.bento_image, class: "card-img-top", alt: bento.name %>
              <div class="card-body d-flex flex-column">
                <h5 class="card-title dark m-0"><%= bento.name %></h5>
                <p class="card-text m-0"><%= bento.introduction %></p>
                <% if bento.recommended %>
                  <h6 class="recommended">おすすめ！</h6>
                <% end %>
                <div class="float-right">
                  <%= link_to "詳細を見る",bento_path(bento), class: "btn btn-outline-dark btn-sm stretched-link" %>
                </div>
              </div>
            </div>
          <% end %>
        <% else %>
          <p>商品が存在しません</p>
        <% end %>
      </div>
      <%= paginate @bentoes %>
    </div>
  </div>
</div>